pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{ts,tsx}'
      run: yarn eslint {staged_files}

    fix:
      glob: '*.{ts,tsx}'
      run: yarn prettier --write {staged_files}

commit-msg:
  commands:
    lint-commit:
      run: 'npx commitlint --edit'
